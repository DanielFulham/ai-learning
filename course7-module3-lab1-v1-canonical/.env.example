# Copy to .env and fill in real values.
# .env is gitignored — never commit real keys.

# Required for any lab that uses OpenAI
# Get a key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...